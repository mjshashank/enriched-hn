import type { Env as SharedEnv } from '@enriched-hn/shared';

/**
 * API worker environment bindings
 */
export type Env = SharedEnv;

/**
 * Hono context variables
 */
export type Variables = {
  // Add any context variables here if needed
};
